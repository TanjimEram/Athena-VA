# Secrets — the Groq key lives in .env and must never be committed
.env

# Virtual environment
.venv/

# Python bytecode
__pycache__/

# Per-machine UI state (orb position)
.athena_ui_state.json

# Per-machine settings written by the settings dashboard
settings.json

# Google OAuth token (a live credential - grants access to Docs and Gmail)
google_token.json

# Per-run timing measurements
latency_log.jsonl
